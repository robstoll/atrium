buildscript {
    ext.publish_description = 'Represents a convenience module which merely bundles dependencies'
}

dependencies {
    compile ownModule('api-cc-de_CH')
    compile ownModule('translations-de_CH')
    compile ownModule('domain-robstoll')
    compile ownModule('core-robstoll')

    testCompile ownModule('verbs')
}
