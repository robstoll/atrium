buildscript {
    ext.publish_description = 'Represents a convenience module which merely bundles dependencies'
}

dependencies {
    compile project(":${rootProject.name}-api-code-completion-en_UK")
    compile project(":${rootProject.name}-translations-en_UK")
    compile project(":${rootProject.name}-core-impl-robstoll")
}
