buildscript {
    ext.publish_description = 'Represents a convenience module which merely bundles dependencies'
}

dependencies {
    compile project(":${rootProject.name}-api-cc-en_UK")
    compile project(":${rootProject.name}-translations-en_UK")
    compile project(":${rootProject.name}-domain-robstoll")
    compile project(":${rootProject.name}-core-robstoll")

    testCompile project(":${rootProject.name}-verbs")
}
