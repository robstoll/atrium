buildscript {
    ext {
        publish_artifactId = 'atrium-api'
        publish_version = atrium_version
        publish_description = 'API of Atrium -- an assertion framework for Kotlin.'
    }
}
apply from: '../gradle/scripts/publish.gradle'

dokka {
    sourceDirs = files('src/main/kotlin')
}
