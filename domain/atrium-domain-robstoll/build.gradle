buildscript {
    ext.publish_description = 'robstoll\'s <rstoll@tutteli.ch> implementation of the domain (assertion functions and builders) of Atrium'
}

dependencies {
    compile ownModule('domain-api')
    compile ownModule('domain-robstoll-lib')
}

dokka {
    sourceDirs = files(
        srcKotlin,
        srcOwnModule('domain-api'),
        srcOwnModule('core-api'),
    )
}
