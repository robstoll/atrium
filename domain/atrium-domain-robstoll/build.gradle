description = 'robstoll\'s <rstoll@tutteli.ch> implementation of the domain (assertion functions and builders) of Atrium'

dependencies {
    compile prefixedProject('domain-api')
    compile prefixedProject('domain-robstoll-lib')
}
