buildscript {
    ext.publish_description = 'API of the domain of Atrium.'
}

dependencies {
    compile ownModule('core-api')
}

dokka {
    sourceDirs = files(
        srcKotlin,
        srcOwnModule('core-api'),
    )
}
