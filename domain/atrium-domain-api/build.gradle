buildscript {
    ext.publish_description = 'API of the domain of Atrium.'
}

dependencies {
    compile prefixedProject('core-api')

    //TODO remove with 1.0.0
    // it is up to the consumer of atrium-assertions which atrium-translations module is used at runtime
    compileOnly prefixedProject('translations-en_UK')
}
