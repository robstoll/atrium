description = 'API of the core of Atrium.'

dependencies {
    compile "ch.tutteli:kbox:$kbox_version", withoutKotlin
    testCompile prefixedProject('cc-infix-en_GB-robstoll')
    testCompile prefixedProject('verbs-internal')
}
