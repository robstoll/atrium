buildscript {
    ext.publish_description = 'robstoll\'s <rstoll@tutteli.ch> implementation of the core of Atrium.'
}

dependencies {
    compile ownModule('core-api')
    compile ownModule('core-robstoll-lib')
}

dokka {
    sourceDirs = files(srcKotlin, srcOwnModule('core-api'))
}
