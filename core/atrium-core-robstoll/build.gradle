buildscript {
    ext.publish_description = 'robstoll\'s <rstoll@tutteli.ch> implementation of the core of Atrium.'
}

dependencies {
    compile prefixedProject('core-api')
    compile prefixedProject('core-robstoll-lib')
}
