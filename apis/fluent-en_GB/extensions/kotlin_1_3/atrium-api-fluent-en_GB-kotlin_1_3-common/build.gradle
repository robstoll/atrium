description = 'Kotlin 1.3 specific assertion functions and builders for fluent-en_GB-common -- will be merged into fluent-en_GB-common with 1.0.0'

dependencies {
    api prefixedProject('api-fluent-en_GB-common')
    api prefixedProject('logic-kotlin_1_3-common')

    testImplementation prefixedProject('specs-common')
}
