description = "Deprecated translation project - use ${rootProject.name}-translations-de_CH instead."

dependencies {
    compile prefixedProject('core-api-jvm')
    compile prefixedProject('translations-de_CH')
}
