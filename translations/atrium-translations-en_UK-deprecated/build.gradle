description = "Deprecated translation project - use ${rootProject.name}-translations-en_GB instead."

dependencies {
    compile prefixedProject('core-api')
    compile prefixedProject('translations-en_GB')
}
